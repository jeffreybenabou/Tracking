import {StyleSheet} from 'react-native';

const regularButton = StyleSheet.create({
  button: {
    backgroundColor: '#043779',
    height: 50,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    color: 'white',
    fontSize: 20,
  },
});

export {regularButton};
